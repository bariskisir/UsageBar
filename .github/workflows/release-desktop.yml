name: Release Desktop

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false

env:
  DOTNET_NOLOGO: true

jobs:
  test:
    name: Test
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - run: dotnet test src/UsageBar.Tests/UsageBar.Tests.csproj -c Release

  validate-platforms:
    name: Validate ${{ matrix.name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Windows
            os: windows-latest
            project: src/UsageBar.Windows/UsageBar.Windows.csproj
          - name: Linux
            os: ubuntu-latest
            project: src/UsageBar.Linux/UsageBar.Linux.csproj
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore workloads
        run: dotnet workload restore ${{ matrix.project }}

      - run: dotnet build ${{ matrix.project }} -c Release

  build-windows:
    name: Build Windows ${{ matrix.arch }}
    runs-on: windows-latest
    needs: [test, validate-platforms]
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runtime: win-x64
            inno_arch: x64os
          - arch: arm64
            runtime: win-arm64
            inno_arch: arm64

    steps:
      - uses: actions/checkout@v4

      - name: Set release version
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}"
          if ($version.StartsWith("v")) { $version = $version.Substring(1) }
          Add-Content -Path $env:GITHUB_ENV -Value "APP_VERSION=$version"

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Publish Windows
        run: >
          dotnet publish src/UsageBar.Windows/UsageBar.Windows.csproj
          -c Release
          -r ${{ matrix.runtime }}
          --self-contained true
          -o artifacts/publish/${{ matrix.runtime }}
          -p:PublishSingleFile=true
          -p:EnableCompressionInSingleFile=true
          -p:Version=${{ env.APP_VERSION }}

      - run: choco install innosetup --no-progress --yes

      - name: Build installer
        shell: pwsh
        run: |
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (-not (Test-Path $iscc)) {
            $iscc = (Get-Command iscc.exe -ErrorAction Stop).Source
          }
          & $iscc installer/UsageBar.iss `
            /DAppVersion="$env:APP_VERSION" `
            /DAppArch="${{ matrix.arch }}" `
            /DInnoArch="${{ matrix.inno_arch }}" `
            /DSourceDir="$PWD\artifacts\publish\${{ matrix.runtime }}" `
            /DOutputDir="$PWD\artifacts\installer"

      - uses: actions/upload-artifact@v4
        with:
          name: usagebar-windows-${{ matrix.arch }}
          path: |
            artifacts/installer/*_${{ matrix.arch }}-setup.exe
          if-no-files-found: error

  build-linux:
    name: Build Linux ${{ matrix.arch }}
    needs: [test, validate-platforms]
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x64
            runtime: linux-x64
            appimage_arch: x86_64
          - arch: arm64
            runtime: linux-arm64
            appimage_arch: aarch64

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set release version
        shell: bash
        run: |
          version="${GITHUB_REF_NAME#v}"
          echo "APP_VERSION=$version" >> "$GITHUB_ENV"

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Publish Linux
        run: >
          dotnet publish src/UsageBar.Linux/UsageBar.Linux.csproj
          -c Release
          -r ${{ matrix.runtime }}
          --self-contained true
          -o artifacts/publish/${{ matrix.runtime }}
          -p:PublishSingleFile=true
          -p:EnableCompressionInSingleFile=true
          -p:Version=${{ env.APP_VERSION }}

      - name: Build AppImage
        shell: bash
        run: |
          packaging/linux/build-appimage.sh \
            artifacts/publish/${{ matrix.runtime }}/UsageBar.Linux \
            "$APP_VERSION" \
            ${{ matrix.appimage_arch }} \
            artifacts/release

      - uses: actions/upload-artifact@v4
        with:
          name: usagebar-linux-${{ matrix.arch }}
          path: artifacts/release/UsageBar-*_*.AppImage
          if-no-files-found: error

  publish:
    name: Publish GitHub Release
    runs-on: ubuntu-latest
    needs: [build-windows, build-linux]

    steps:
      - uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - run: find release-assets -maxdepth 5 -type f -print

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: UsageBar ${{ github.ref_name }}
          files: |
            release-assets/**/*_*-setup.exe
            release-assets/**/*.AppImage
          fail_on_unmatched_files: true
          generate_release_notes: true
